<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.em.app">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:name="com.em.app.MediaApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Light"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.em.app.activities.home.HomeActivity"
            android:launchMode="singleInstance">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.em.app.activities.BaseActivity" />

        <service android:name="com.em.ui.notifications.NotificationService" />
        <receiver
            android:name="com.em.ui.notifications.MediaNotificationManager"
            android:exported="false">
            <intent-filter>
                <action android:name="noti_play" />
            </intent-filter>
        </receiver>
    </application>

</manifest>